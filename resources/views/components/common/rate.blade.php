<div class="flex gap-x-4 items-center">
    <div class="flex">
        <x-svg.star starClass="{{ $starClass ?? '' }}" />
        <x-svg.star starClass="{{ $starClass ?? '' }}" />
        <x-svg.star starClass="{{ $starClass ?? '' }}" />
        <x-svg.star starClass="{{ $starClass ?? '' }}" />
        <x-svg.star starClass="{{ $starClass ?? '' }}" />
    </div>
    <span class="text-[12px] text-gray-400 {{ $rateClass ?? '' }}">
        4.5 (149)
    </span>
</div>
